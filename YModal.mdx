---
name: YModal
menu: YBasic
---

import { Playground } from 'docz';
import {useState} from 'react';
import YModal from './src/YBasic/YModal/YModal.js';
import { YButtonPrimary } from './src/YBasic/YButton/YButton.js';
import SourceDisplay from './SourceDisplay';
import source from '!!raw-loader!./src/YBasic/YModal/YModal.js'
import FunctionChild from './FunctionChild.js';

Quality: Sophistication

# YModal
---

## position: fixed;

<Playground>
	<FunctionChild>
		{() => {
			const [open, setOpen] = useState(false);
			return (
			<>
				<pre style={{margin: 0}}>{'(default)'}</pre>
				<YButtonPrimary onClick={() => setOpen(!open)}>
					Click me
				</YButtonPrimary>
				{
					open &&
					<YModal>
						Hello, I am a basic modal, close me with <YButtonPrimary onClick={() => setOpen(false)}>this button</YButtonPrimary>
					</YModal>
				}
			</>
			);
		}}
	</FunctionChild>
</Playground>

<Playground>
	<FunctionChild>
		{() => {
			const [open, setOpen] = useState(false);
			return (
			<>
			<pre style={{margin: 0}}>{'width: fit-content'}</pre>
				<YButtonPrimary onClick={() => setOpen(!open)}>
					Click me
				</YButtonPrimary>
				{
					open &&
					<YModal child={{style: {width: 'fit-content'}}}>
						Hello, I am a basic modal, close me with <YButtonPrimary onClick={() => setOpen(false)}>this button</YButtonPrimary>
					</YModal>
				}
			</>
			);
		}}
	</FunctionChild>
</Playground>

<Playground>
	<FunctionChild>
		{() => {
			const [open, setOpen] = useState(false);
			return (
			<>
				<pre style={{margin: 0}}>{'width: fit-content with outside click'}</pre>
				<YButtonPrimary onClick={() => setOpen(!open)}>
					Click me
				</YButtonPrimary>
				{
					open &&
					<YModal
						onOutsideClick={() => setOpen(false)}
						child={{style: {width: 'fit-content'}}}
					>
						Hello, I am a basic modal, close me with <YButtonPrimary onClick={() => setOpen(false)}>this button</YButtonPrimary> or by clicking outside!
					</YModal>
				}
			</>
			);
		}}
	</FunctionChild>
</Playground>

<Playground>
	<FunctionChild>
		{() => {
			const [open, setOpen] = useState(false);
			return (
			<>
				<pre style={{margin: 0}}>{'with height overflow'}</pre>
				<YButtonPrimary onClick={() => setOpen(!open)}>
					Click me
				</YButtonPrimary>
				{
					open &&
					<YModal
						onOutsideClick={() => setOpen(false)}
						child={{style: {width: 'fit-content'}}}
					>
						Hello, I am a basic modal,
						<div>
							{Array(100).fill().map((_, i) => <span key={'key'+i}>and I'm just way too verbose</span>)}
						</div>
						{Array(100).fill().map((_, i) => <div key={'key'+i}>and I'm just way too verbose</div>)}
						, close me with
						<YButtonPrimary onClick={() => setOpen(false)}>this button!</YButtonPrimary>
					</YModal>
				}
			</>
			);
		}}
	</FunctionChild>
</Playground>

## position: absolute;
<Playground>
	<>
	<pre style={{margin: 0}}>{'(default))'}</pre>
	<div style={{position: 'relative', height: '400px'}}>
		<YModal
		style={{
			position: 'absolute',
			zIndex: 'unset'
		}}>More like resolute!</YModal>
	</div>
	</>
</Playground>

<Playground>
	<>
	<pre style={{margin: 0}}>{'with height overflow'}</pre>
	<div style={{position: 'relative', height: '400px'}}>
		<YModal
		style={{
		position: 'absolute',
		zIndex: 'unset'
		}}>
			<div>
				{Array(100).fill().map((_, i) => <span key={'key'+i}>Wow there's just so many words!</span>)}
			</div>
			{Array(100).fill().map((_, i) => <div key={'key'+i}>And just even more!</div>)}
			</YModal>
	</div>
	</>
</Playground>

# Source
./src/YBasic/YModal/YModal.js
<SourceDisplay source={source} />